import { ForRoleDirective } from './for-role.directive';

describe('ForRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new ForRoleDirective();
    expect(directive).toBeTruthy();
  });
});
